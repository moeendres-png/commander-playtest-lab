# Legacy phase artifacts

Historical Phase 2–9 reports, test-result snapshots and validation outputs are archived here for provenance and recovery. They are not current project truth and must not override current Drive routing or current GitHub `main`.

A small number of generated validation filenames remain at repository root because current audit/runtime code still addresses those exact paths. Their presence is a compatibility contract, not a Current-Truth claim.

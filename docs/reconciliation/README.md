# 1.13.3 reconciliation — historical record

This document records the historical Drive-derived 1.13.3 reconciliation lineage.

The active software source of truth is the current `main` branch. No further merge, replay, or reconstruction from the 1.13.3 line is required or permitted as an active development step.

If historical provenance is needed, use the preserved Git/Drive reconciliation evidence. New work must start from the currently verified `main` state (`reconstruct_from_current_main`).

# WS223 dependency-lock compiler input.
#
# Covers every third-party set any CI lane installs (pyproject base +
# dev + api + openai extras, the ci.yml security-audit pins, and the
# build backend needed for `--no-build-isolation` editable installs).
# The local package itself is installed separately with
# `pip install --no-deps --no-build-isolation -e .` and recorded in the
# lock appendix (it is not PyPI-hashable).
pydantic>=2.10,<3
openpyxl>=3.1,<4
PyYAML>=6.0,<7
typer>=0.15,<1
pytest>=8.3,<10
pytest-asyncio>=1.4,<2
hypothesis>=6.120,<7
httpx2>=2.7,<3
jsonschema>=4.23,<5
ruff>=0.9,<1
mypy>=1.14,<2
types-openpyxl>=3.1,<4
types-PyYAML>=6.0,<7
fastapi>=0.115,<1
uvicorn>=0.34,<1
openai>=2.45,<3
openai-agents>=0.18.3,<0.19
pip-audit==2.10.1
cyclonedx-bom==7.3.0
pip-licenses==5.5.5
setuptools>=75
wheel
build

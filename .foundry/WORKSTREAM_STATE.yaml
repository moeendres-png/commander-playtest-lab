schema_version: '2.0'
repository: moeendres-png/commander-playtest-lab
worktree: /home/moeen/code/ws-a1r-authority-retention
branch: architecture/ws-a1r-authority-retention-hygiene-20260910
audit_base_sha: d2bd60885325a00d6ce8d8d1d0ed2ddcde445e68
audit_base_tree: 215611ac8d1ea93353c2fcc4c8d7e06dabb4ca51
state_written_against_head: d2bd60885325a00d6ce8d8d1d0ed2ddcde445e68
validated_head: null
objective: WS-A1R repository authority, pin, pointer and retention hygiene on the accepted 3-repo pre-freeze topology (non-destructive; no mergers/splits/deletions; no license, protection, or pin changes).
in_scope:
- integrations/forge/README.md pin-authority repair (reference manifest, drop stale 2.0.13 pin)
- docs/engine_setup.md pin-table repair (reference manifest, drop stale SHAs)
- config/rules_engines.json additive authority_note (terminology + documentation rule + stale-pointer record; no key touched)
- docs/RETENTION_AND_LIFECYCLE_POLICY.md (canonical lifecycle states, fail-closed deletion eligibility, Appendix G gates)
- docs/EVIDENCE_INDEX_REQUIREMENTS.md (minimum index fields + template; no re-indexing ordered)
- docs/FORK_AGENT_POINTER_SPEC.md (fork pointer design + WS-A1R-F1/F2 follow-up specs; no fork edits)
- tests/unit/test_ws_a1r_pin_authority.py (fail-closed doc/manifest consistency tests)
out_of_scope:
- branch/PR deletion or closure; evidence deletion or migration; history rewrite; force push; rebase
- license selection or LICENSE creation (LICENSE_DECISION = AUTHORITY_GATE)
- branch-protection/ruleset mutation (BRANCH_PROTECTION = AUTHORITY_GATE)
- engine re-pinning incl. Dockerfile defaults (PIN_DIVERGENCE_DOCKERFILES = AUTHORITY_GATE)
- schema renames of primary_engine/secondary_engine (DEFERRED; terminology clarified in docs)
- fork repository edits (WS-A1R-F1/F2 follow-ups)
- provider selection, Architecture Freeze, Production Repository, qualification campaigns
ownership: WS-A1R session (branch architecture/ws-a1r-authority-retention-hygiene-20260910)
dependencies:
- origin/main at d2bd60885325a00d6ce8d8d1d0ed2ddcde445e68 (no drift from audit lock; verified by ls-remote + fetch)
- ARCH-PORTFOLIO-AUDIT-01 findings (output/ in audit workspace; comparison evidence only)
hard_gates:
- no destructive action; no license decision; no protection mutation; no pin change
- manifest edit strictly additive (existing keys byte-identical)
- terminology clarified in docs only; runtime contracts untouched
- all tests green before handoff; no push without explicit user approval; no merge
forbidden_shortcuts:
- second hidden Rules Engine; fabricated evidence; green-theater tests
- closing PRs or deleting branches as drive-by cleanup
- copying SHAs into docs as new pin authority
authority_gates:
- 'LICENSE_DECISION = AUTHORITY_GATE (human/legal; factual input in Retention Policy Appendix G)'
- 'BRANCH_PROTECTION = AUTHORITY_GATE (human governance workstream)'
- 'DESTRUCTIVE_CLEANUP = AUTHORITY_GATE (Coordinator + human per wave)'
- 'PIN_DIVERGENCE_DOCKERFILES = AUTHORITY_GATE (dedicated re-pin + requalification workstream)'
status: ACTIVE
next_action: 'Validate state file with tools/foundry/state.py, run scoped pytest set, commit locally, hand off.'
exact_next_action: Run tools/foundry/state.py validator on .foundry/WORKSTREAM_STATE.yaml plus pytest on the pin-authority and engine-config suites; on green, create focused local commits (no push, no merge); return the WS-A1R handoff.

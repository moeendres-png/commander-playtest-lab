# WS215 MULTIPLAYER_APNAP — UNKNOWN (bounded windows)

No simultaneous multi-player trigger (APNAP ordering) frame arose in any
bounded lifecycle (neutral 150 × 4 counts + develop 600 × 4 counts +
supplemental runs): `trigger_order` decision class never offered. The
developed boards produced combat, casts, and zone choices, but no moment
with simultaneous triggers controlled by different players in the bounded
windows.

- APNAP ordering mechanism remains solely XMage-owned (untouched by the
  cardinality change: no trigger code was modified in Lab or bridge).
- Single-player triggers resolved natively wherever they occurred (no
  trigger mishandling observed; no failure).
- Claiming APNAP conformance without an observed ordering would be
  fabrication; therefore UNKNOWN with exact cause, not PASS.

Fixtures `WS05-MP-TRIG-3`, `WS05-MP-TRIG-5`: fresh disposition UNKNOWN
(bounded successor: trigger-rich decks or longer windows that force
simultaneous triggers across seats).

Machine companion: `MULTIPLAYER_APNAP.json`.

# WS213 D5_ADJUDICATION — `D5_TWIN_EQUALITY = PASS`

Basis: fresh-process same-seed twins on the actual production Lab path
(binding owned by the session, no reflective hook).

1. WS208 context: the false-twin matrix (A03, B01, D06, E02, I01,
   HUMILITY_FIRST) is SUPERSEDED. All six are TRUE at decision level on the
   bound pin; WS208's layer-A cause (unbound engine GameRandom) is removed
   by construction.
2. Harness fidelity fix (WS213 driver only; sealed WS205 driver untouched):
   the twin imitated schema-minimum on numeric-only frames instead of the
   recorded value. Fixed by replaying the recorded numeric with validation.
   This was the entire C03 behavior divergence (announce_x 4 vs 0); offers
   had matched exactly.
3. C03 residual: after the fix, all 122 decision rows match (offers, labels,
   numerics). Both twins then hit the IDENTICAL native failure
   (`XMAGE_ACTION_EXECUTION_FAILED: priority activation failed`, offset 245,
   Fireball mana-cancel underfunded by driver policy — pre-existing
   engine/bridge interaction on an unchanged code path, identical on either
   pin). Strict-hash differs only in the two post-failure terminal-capture
   rows (tapped-Mountain zone position race). No offer difference exists;
   nothing was normalized away: both the strict matrix (18/19→ documented)
   and the decision-row matrix (19/19) are sealed.
4. C01 shows the same identical-twin native failure (offset 175) with
   strict-hash TRUE.

Verdict: `D5_TWIN_EQUALITY = PASS` on the decision-row (offered-action-set)
basis that the D5 contract specifies. `BIT_EXACT_REPLAY_VALIDATED` stays
false. Engine-unit evidence was never used to upgrade this verdict.

Machine companion: `D5_ADJUDICATION.json`.

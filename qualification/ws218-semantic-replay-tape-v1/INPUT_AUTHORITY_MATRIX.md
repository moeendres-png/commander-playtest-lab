# WS218 INPUT_AUTHORITY_MATRIX

Source lock: Lab `moeendres-png/commander-playtest-lab` @ `67db073367853da7295ae06642f38a73db464dba`
(tree `a41ad3959caee03db59ffa15bdd44413d58ca766`, branch `ws218/semantic-replay-tape-v1-20260915`).
XMage production authority `db134b9737e951367d65ef5806ad986319cc73ab` (engine 1.4.61,
`XmageProvider.ENGINE_COMMIT`, decision protocol `xmage-external-decision-protocol-1.0.0`,
transport protocol `2.0.0`). Forge divided core `c4d67145a6f9902e031a11dde5c33c60f51ed08d`
read-only reference. WS217 `e152688a33bf69a840b74ae86149d881e64538ec` (replay NOT CLAIMED,
no bridge legality, principal scoping PASS) used only as future-consumer compatibility
reference; no Forge integration in WS218.

| # | Input authority | Finding (freshly verified) | WS218 use |
|---|---|---|---|
| 1 | WS213 `SEMANTIC_REPLAY_IMPACT.md/.json` | PARTIAL: explicit seed+explicit+require per run, `getRulesRandomCalls` accounting, full `decision_requested`/`decision_accepted` transcript; absent: checkpoint schema, replay consumer, validation gate; twins != replay | Preserved; WS218 builds the absent contract; injection ban retained |
| 2 | WS213 `RULES_SEED_BINDING.md/.json`, `XmageFullGameSession` ctor | `setRulesSeed+setRequireExplicitSeed` post-construction pre-start; live `rules_seed_binding` proof per payload; RandomUtil retired as authority | Tape `rng_contract` + per-step calls coordinates; results never injected |
| 3 | WS215 `SEMANTIC_REPLAY_IMPACT.md`, per-count twins, `WS215_MATRIX.json`, `WS215_OBSERVATIONS.json` | PARTIAL retained per-count 2-5P; 8/8 twin MATCH + 8/8 distinct-seed DIVERGE; oracle 9985/0; transcript hash excludes UUIDs; `last_submission` raw UUIDs differ per process (proven nonsemantic) | Twin equality is determinism only; tape adds legal-set/observation/RNG/event/checkpoint compare + consumer |
| 4 | `XmageFullGameSession` | One game/process; seed bound before start; `statusPayload` carries `rules_seed_binding`, turn, outcomes, `can_concede` vector; `resultPayload` carries transcript+seed+binding; no state restore API | Recorder observes status+decision; consumer reconstructs from manifest; no zone/life/ledger/stack injection |
| 5 | `XmageFullGameDecisionController` | Blocking fail-closed handoff; `submit` validates revision/actor/membership/bounds/numeric; `decision_id=stableId(gameId,offset,actor,class)` (process-local: gameId+actor are per-JVM UUIDs); transcript `decision_requested`/`decision_accepted` with redacted types/labels; `redactObjectIds`+`stableId` | Tape revision = `decision_offset` (stable); raw `decision_id`/actor UUID excluded from digests; resubmit uses CURRENT native ids |
| 6 | `XmageFullGamePlayer` option construction | `option_id` is process-local for object/ability/attack/block paths (`UUID.toString` or `stableId` over UUIDs) and stable for mulligan/boolean/choice-key/mana-pool/pile slots; labels/metadata carry semantic content (`source_name`, ability text, `choice_key`, `value bool`, pile names, `mana_type`) | Fingerprint uses type+redacted label+semantic metadata+observation-joined public projection; raw ids never hashed |
| 7 | `XmageFullGameStateRedactor.actorView` | Actor-scoped: actor hand/mana/land-plays only; opponents counts+public zones; battlefield permanents with public characteristics; `granted_library` only inside entitled window; `commander_status` public damage/tax | Observation digests map UUIDs to seat/occurrence keys; public digest drops hand/mana/granted; hashes only on pilot surfaces |
| 8 | `XmageFullGameActionProjection` | Decision-scoped projection only; `action_id=decision_id:option_id`; revision binding precedes actor binding; no legality computed | Tape is NOT a second legality engine; consumer never filters; projection vocabulary reused, never widened |
| 9 | `XmageFullGameJsonlBridge` capabilities | Full-game lane `replay_supported=false`, `legal_actions/action_submission=false`, `event_log=false`; one game/process enforced; concede offer/execute via native `canConcede`/`concede` | Bridge `replay_supported` stays false; tape lane capability is separate truth; concessions are lifecycle steps via native API |
| 10 | `XmageAuditEventLog` | Bridge lifecycle audit only, not exhaustive GameEvent tap; `stateHash` naive `toString` (not semantic) | Event tape defined as decision-offset range + canonical transcript/state digest, not raw GameEvent equality; debug strings excluded |
| 11 | `ExternalPilotDecisionPolicy` | Production discretionary authority; 17 classes; deterministic rng `sha256(seed:seat:offset:class)`; mana liveness guard; target tiebreak by score+label+id (residual UUID element noted) | Recorder uses this policy; consumer BYPASSES pilot (resubmits recorded semantics); pilot derivation bound in manifest |
| 12 | `XmageFullGameRunner` + `semantic_transcript` | Same-seed gate compares redacted semantic transcript + raw hash; `bit_exact_replay_validated=false` honest | Replaced by tape consumer for PASS; twin gate retained as determinism control only |
| 13 | Protocol `export_replay` / `EngineCapabilityHandshake.replay_supported` | False on both full-game and B4 lanes; `export_replay` fails closed without capability | Untouched; tape does not use `export_replay`; no capability widened |
| 14 | D5 decision-row comparison (`Ws215LifecycleProbe.stableTranscriptCanonical`, `stableActionKey`) | Rows exclude UUIDs; harness ranks by stable content; residual ties among identical options harmless for reachability but INSUFFICIENT for replay (must fail closed on ambiguity) | Tape resolver fails closed on 0 or >1 matches; never first-option |
| 15 | Batch/process isolation (`full_game_batch.py`, `JsonLineBridgeClient`, one-game-per-process) | Content-addressed batch records; atomic writes (tmp+replace); isolated JVM per game; timeouts | Recorder/consumer reuse atomic-write + fresh-process discipline; incomplete artifacts marked, never sealed |
| 16 | WS217 successor spec (`ws217-lab-successor-spec.json`) | Families `TARGET_SELECTION`/`DIVIDED_ALLOCATION`/`NUMBER_CHOICE`; opaque targets; Core validates before mutation; principal scoping; replay NOT CLAIMED | Tape decision frame is engine-neutral (class string, principal seat, revision, fingerprints, numeric bounds); no XMage-only families assumed |
| 17 | Existing `replay.py` / `EngineReplay` | Snapshot-reduction validator for tactical/internal states; requires `internal_state_after` per event; unrelated to full-game lane | Not reused for full-game tape (would be state injection); documented as out-of-scope |
| 18 | Canonicalization helpers (none versioned) | Ad-hoc `_semantic_text` redaction only; no persisted canonical version | New `semantic-canonical-1.0.0` + persisted versions in every digest input |

Machine companion: `INPUT_AUTHORITY_MATRIX.json`.

# WS218 HIDDEN_INFORMATION_REPLAY

Principal boundaries preserved end to end. Actor views remain the WS215
actor-scoped projections (actor hand/mana only; opponents counts + public
zones; window-only granted library; public commander rows). Tape artifacts
contain zero raw UUIDs and no hand/mana/granted arrays (scan of sealed
tapes). Digests are hashes; pilot-facing consumer returns pass/steps/
seat-outcomes only; divergence diagnostics carry class/seat/revision/calls
(leak scan over all 19 tamper diagnostics: no card names). Option
fingerprints join only entitled public projections. Privileged tapes may
carry redacted actor-entitled labels + hashes, never served cross-principal.
WS215 honeycard/oracle lineage reused as construction argument; name-canary
negatives remain S7 (cited, not implemented or upgraded per addendum).

Machine companion: `HIDDEN_INFORMATION_REPLAY.json`.

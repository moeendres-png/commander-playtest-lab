# REQUALIFICATION RECORD — WS232 predicates, R26 re-baseline (2026-09-23)

Authorized by: Coordinator PR #240 qualification-retention remediation
(executor workstream: real-deck E2E campaign), branch
`opencode/real-deck-e2e-gate-20260923` (sole writer; PR #239 untouched;
no rebase/squash/history rewrite).

## Affected predicates

42 of 47 (`actual_card` 29 = CARD_01..29, `micro_rules` 13 = combat,
continuous_effects, control, copy, layers, modes, prevention,
replacement, rules_randomness, stack, state_based_actions, triggers,
zone_changes). Every STATIC_FAIL is carried by C1-owned binds only;
all other binds (engine commit db134b97, provider identity, protocol
2.0.0, seeds, fixture bytes incl. all 42 fixture identities,
rules-seed binding, tape schema, session/bridge construction blobs,
action projection) match unchanged. The 5 `replay_rng` predicates
never failed (no replay/tape/RNG path touched).

## Old → new source identity (old verified = origin/main bytes)

- `src/commander_lab/engine/rules/full_game.py`:
  `7e2597ee...` (origin/main `ca7fd4a4`, verified byte-equal before
  replacement) → `3c4bdf57...` (C1 workstream).
- `src/commander_lab/agents/pilots.py`:
  `1a45f202...` (origin/main, verified) → `5c0a16c3...` (C1).
- `engine-bridge/.../XmageFullGamePlayer.java`:
  `243ba8f6...` (origin/main, verified) → `fcf705ad...` (C1).

84 binds updated (29×2 CARD + 13×2 MICRO), old values asserted before
replacement; no other bind touched. Diff of RETENTION_PREDICATES.json
is SHA-only (no structural churn).

## Behavior-vs-bytes determination (diff ca7fd4a4 → HEAD, 3 files)

Honestly NOT behavior-identical at the harness selection layer (that is
the C1 repair surface); engine mechanisms and card implementations are
byte-untouched (pin db134b97, no card code, no engine sources):

- full_game.py: pilot discretionary ranking among engine-authorized
  options (affordability withholding incl. mid-payment, London/library
  bottom shape split, modal viability preference, native-flag mana
  pool filter, mana/priority no-progress guards with latches,
  content-stable view identities, close() disposition + fail-closed
  smoke). Engine offer sets, execution, validation, RNG, seeds,
  hidden-information scoping unchanged; previously-fatal paths now map
  to pass; no legality reconstructed (authority adjudication: ranking,
  not filtering; see C1 handoff).
- pilots.py: one-line tiebreak in choose_bottom_cards
  (action_id → card_name); value ordering unchanged.
- XmageFullGamePlayer.java: additive native projections (cost facts,
  pool affordance via ManaCost.testPay, per-mode target availability,
  zone/zone_index, trigger ordering) + graceful abort→pass for
  payment-cancel/empty-target/no-viable-mode (previously fatal);
  offers and execution paths otherwise identical.

## Impact adjudication per family

- CARD_01..29: STATIC rebased (binds match current tree).
  RUNTIME stays EXPLICIT UNKNOWN (R21 sealed cause extended by this
  record): no per-card live reruns exist on current bytes; C1's 4P
  real-deck evidence is contract-barred from discharging 2/3/5 cells
  and is not fixture-specific. No promotion of import/construction
  into behavior PASS. No card-name logic introduced.
- MICRO (13, incl. costs/mana/payment/priority/targets/modes that C1
  provokes live): STATIC rebased. RUNTIME stays EXPLICIT UNKNOWN:
  C1 terminal games exercised these mechanisms live on final bytes
  (supporting evidence only — modes, targets, mana payment, priority,
  costs, triggers, replacement, stack all traversed in 4P real games),
  but required N-cells are 2/3/5 fixture-specific reruns, which remain
  a residual campaign. No cell discharged.
- No TARGETED_RERUN_REQUIRED cell is dischargeable by C1 evidence;
  none executed beyond the already-accepted C1 terminal/replay/batch
  (not repeated for retention). 4P cells are not in any predicate's
  required set.

## Retention condition and rerun evidence (on new bytes)

- Full engine-bridge suite 200 run / 0 failures / 0 errors / 1 skipped
  on final bytes (incl. PoolCovers direction guard + all action
  projection/priority/mulligan consumers).
- Focused Python suites green on integrated bytes (retention gate,
  affordability, bottom shapes, pilots, variable-player/flag
  contract, full-game, decision matrix, replay tape, counterfactual,
  shutdown, ws223, ws78).
- Predicate checker `bin/check_predicates.py`: 47/47 STATIC_PASS
  (RESULTS regenerated by the authorized checker, not hand-edited).
- CI gate test `tests/qualification/test_ws232_retention_predicates.py`
  green (includes static-evaluation 47/47 + engine-pin + workload
  shape assertions).
- Live strict smoke (25 authoritative decisions + observed graceful
  shutdown) green on final bytes.

## Manifest update

`RETENTION_PREDICATES.json`: 84 binds updated (verified old values).
`RETENTION_PREDICATE_RESULTS.json`: regenerated by checker; 47/47.
No other retention/disposition/fixture file touched.

## Disposition

No fixture/status change: `N_SCOPED_DISPOSITION_R21.json`,
`WORKLOAD_DERIVATION.json`, and the predicate set are untouched; all
42 impacted predicates retain STATIC_PASS with their 2/3/5 cells
EXPLICIT UNKNOWN (sealed cause: this record). No mapping promotion.
Residual: N-scoped per-card/per-fixture rerun campaign (2/3/5).

# REQUALIFICATION RECORD — WS232 predicates, R21 remediation (2026-09-21)

Authorized by: Coordinator authorization "PR #207 — COMPLETE INTEGRATION
REMEDIATION" (§5, WS17 retention requalification), branch
`cpl/ci-cardinality-lane-20260921` (successor ownership; WS33 has no
active Lab-side writer on these files).

## Affected predicates

All 47 (`actual_card` 29, `micro_rules` 13, `replay_rng` 5). Every
STATIC_FAIL is carried by exactly the same two binds; all other binds
(engine commit db134b97, provider identity, protocol 2.0.0, seeds,
fixture bytes, rules-seed binding, tape schema) match unchanged.

## Old → new source identity

- `engine-bridge/.../XmageFullGameSession.java`:
  `edf4ca1a...` (recorded at successor checkpoint 291dc89a)
  → `d551cf64...` (R19 bytes, unchanged by R21 remediation).
- `src/commander_lab/engine/rules/full_game.py`:
  `9b8e9eaf...` (recorded at 291dc89a)
  → `e748070f...` (R19 bytes + R21 message-only corrections
  "two to five" → "two to six", zero behavior change).

## Behavior-vs-bytes determination (diff 291dc89a → HEAD, both files)

- Session: 2 changed lines (javadoc + `MAX_PLAYERS` 5→6). Gate
  `size > MAX_PLAYERS` evaluates identically for sizes 0–5; only 6 is
  newly admitted. No other line touched.
- full_game.py: bound widenings `le=5→6` / `>5→>6` / `MAX 5→6`
  (identical acceptance + downstream for 2–5), one docstring line,
  pure reformatting of the multi_amount block (no logic change),
  two message-only corrections (R21).
- Conclusion: EVIDENCE/INDEX-BYTE change with a strictly additive
  admission change; 2/3/5P retained paths behavior-identical by
  construction. No card implementation, seed, RNG, replay, or
  hidden-information path touched.

## Retention condition and rerun evidence (on new bytes)

- R19 live gates 2–6 PASS (sealed R19 evidence: bridge suites,
  live 6P smoke 55 + full gate 7284 PASS winner seat 2, replay MATCH;
  successor live 2/3/4/5P gates PASS).
- R21: full engine-bridge suite 156/156 green on final bytes (incl.
  remediated 6P contract tests + preserved 7P/0P/1P negative controls);
  python lane suites re-run on final bytes before commit.
- Negative controls preserved: 0P/1P/7P fail closed (Java), 7P
  fail-closed probe (CI lane + python).

## Manifest update

`RETENTION_PREDICATES.json`: 89 binds updated to the new SHAs above
(old values verified before replacement; no other bind touched).
`RETENTION_PREDICATE_RESULTS.json` regenerated by the authorized
checker (`bin/check_predicates.py`); static 47/47 required.

## Addendum R21b — ruff-format whitespace re-baseline (tool drift)

CI `ruff format --check .` (ruff 0.16.8, locked) flagged 14 files untouched
since before this branch (proven pre-existing: same flags on stashed base).
`ruff format` applied to exactly those files. Six reformatted files carry
retention binds (`agents/pilots.py` in 29 card predicates;
`semantic_replay/{recorder,consumer,canonicalization,fingerprint,capability}.py`
in 5 replay_rng predicates). For each: AST dump old-vs-new proven IDENTICAL
(`ast.dump` equality; formatting-only, zero behavior change). Binds
re-baselined (29 + 25). `tape_helpers.py` reformatted AST-identical, unbound.
## Addendum R21c — mypy-strict type-only re-baseline (tool drift)

CI `mypy src/commander_lab` (mypy 1.20.2, locked) reported 15 errors in 5
files, proven pre-existing (identical on stashed base). All fixes are
type-system-only with zero runtime change: `Final` qualifier (tape),
declaration-only `config: PilotConfig` (pilots mixin), `cast(int, raw)`
after `isinstance` proof (full_game `_required_bound`), `dict[str, Any]`
annotations + identity `cast()` (recorder/consumer contexts),
`int | None` → `int` for always-`int(...)` turn locals. Bound-file SHAs
re-baselined again (pilots/full_game/recorder/consumer/tape); checker
value-resolver and retention test regex widened to accept `: Final`
(value lock unchanged). Checker back to static 47/47. No predicate
deleted, no UNKNOWN as PASS.

## Addendum R21d — Finding-A next-projection fail-closed (behavioral, scoped)

Coordinator Finding A changed `XmageFullGameSession.submitAction`'s
next-decision projection path: silent `next_actions: []` on
`ProjectionException` → explicit `next_actions_status`
(`projected` / `no_pending_decision` / `projection_failed` +
`next_actions_projection_error`), extracted as unit-testable
`nextActionsPayload` (no engine needed for the regression).
Behavior determination: for all retained n-cells the projection
SUCCEEDS (status `projected`), so retained construction/projection
behavior is identical; only the previously-silent failure path gained
an explicit signal (no rollback implied; `executed_*` facts preserved).
Rerun evidence on final bytes: new negative regression 4/4
(`XmageFullGameNextActionsProjectionTest`: null/malformed/blank/projected),
live generic-submission suite 4/4 (real engine, exercises the modified
`submitAction` path end-to-end), full bridge suite 160/160 on final bytes.
Session bind re-baselined to the fixed bytes.

## Addendum R22 — micro RERUN retraction + disposition join (evidence integrity)

Coordinator R22-follow-up: the 39 micro_rules cells first labeled RERUN
pointed at generic cardinality smokes (25/25/45 decisions, broad decision
classes only — no fixture_id, no per-micro-rule assertions for e.g.
MICRO_LAYERS/MICRO_TRIGGERS/MICRO_STATE_BASED_ACTIONS). Retracted to
UNKNOWN with the smokes cited as supporting cardinality evidence only.
Disposition now 141 UNKNOWN / 0 RERUN. `test_n_scoped_disposition_join_complete`
mechanically enforces 47×3 coverage, status vocabulary, reason presence,
and fixture/count correspondence for any future RERUN (negative-control
proven with a bogus pointer). STATIC_PASS unchanged (47/47); no behavioral
PASS manufactured; residual N-scoped fixture campaign stays explicit.

## Addendum R23 — free-mulligan count + zone-counts projection (behavioral, fixture-driven)

FULL107 WS05 execution exposed a genuine bridge defect: the full-game
lane granted one free London mulligan at EVERY count
(`MulliganType.LONDON.getMulligan(1)`), zeroing the 2P bottom count the
WS05-CMD-MULL-2 fixture requires. Fix: free count follows CR 102.1
multiplayer (3+ players) — 2P gets 0, 3..6P keep 1. Same change adds a
read-only public-zone-counts projection (`zoneCountsPayload`: counts
only, no card identities — no hidden-information exposure) used to
verify bottom counts via library size. Behavior delta confined to 2P
bottom counts (previously free, now 1) and the additive projection;
3..6P paths identical. Rerun evidence on final bytes: WS05-CMD-MULL-2/4
fixture-faithful live runs PASS (exact decks/procedure/assertions),
full bridge suite green. 2P lane-gate supporting evidence now carries
this delta note; N-scoped cells stay explicitly UNKNOWN.

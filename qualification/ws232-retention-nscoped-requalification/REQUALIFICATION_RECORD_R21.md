# REQUALIFICATION RECORD — WS232 predicates, R21 remediation (2026-09-21)

Authorized by: Coordinator authorization "PR #207 — COMPLETE INTEGRATION
REMEDIATION" (§5, WS17 retention requalification), branch
`cpl/ci-cardinality-lane-20260921` (successor ownership; WS33 has no
active Lab-side writer on these files).

## Affected predicates

All 47 (`actual_card` 29, `micro_rules` 13, `replay_rng` 5). Every
STATIC_FAIL is carried by exactly the same two binds; all other binds
(engine commit db134b97, provider identity, protocol 2.0.0, seeds,
fixture bytes, rules-seed binding, tape schema) match unchanged.

## Old → new source identity

- `engine-bridge/.../XmageFullGameSession.java`:
  `edf4ca1a...` (recorded at successor checkpoint 291dc89a)
  → `d551cf64...` (R19 bytes, unchanged by R21 remediation).
- `src/commander_lab/engine/rules/full_game.py`:
  `9b8e9eaf...` (recorded at 291dc89a)
  → `e748070f...` (R19 bytes + R21 message-only corrections
  "two to five" → "two to six", zero behavior change).

## Behavior-vs-bytes determination (diff 291dc89a → HEAD, both files)

- Session: 2 changed lines (javadoc + `MAX_PLAYERS` 5→6). Gate
  `size > MAX_PLAYERS` evaluates identically for sizes 0–5; only 6 is
  newly admitted. No other line touched.
- full_game.py: bound widenings `le=5→6` / `>5→>6` / `MAX 5→6`
  (identical acceptance + downstream for 2–5), one docstring line,
  pure reformatting of the multi_amount block (no logic change),
  two message-only corrections (R21).
- Conclusion: EVIDENCE/INDEX-BYTE change with a strictly additive
  admission change; 2/3/5P retained paths behavior-identical by
  construction. No card implementation, seed, RNG, replay, or
  hidden-information path touched.

## Retention condition and rerun evidence (on new bytes)

- R19 live gates 2–6 PASS (sealed R19 evidence: bridge suites,
  live 6P smoke 55 + full gate 7284 PASS winner seat 2, replay MATCH;
  successor live 2/3/4/5P gates PASS).
- R21: full engine-bridge suite 156/156 green on final bytes (incl.
  remediated 6P contract tests + preserved 7P/0P/1P negative controls);
  python lane suites re-run on final bytes before commit.
- Negative controls preserved: 0P/1P/7P fail closed (Java), 7P
  fail-closed probe (CI lane + python).

## Manifest update

`RETENTION_PREDICATES.json`: 89 binds updated to the new SHAs above
(old values verified before replacement; no other bind touched).
`RETENTION_PREDICATE_RESULTS.json` regenerated by the authorized
checker (`bin/check_predicates.py`); static 47/47 required.
No predicate deleted, no UNKNOWN classified as PASS.

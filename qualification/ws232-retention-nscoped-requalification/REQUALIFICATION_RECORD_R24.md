# REQUALIFICATION RECORD — WS232 predicates, R24 re-baseline (2026-09-22)

Authorized by: Phase C authority grant (Workstream 2: native-state injection
capability), branch `cpl/full107-native-injection-20260922` (sole writer;
all other worktrees/branches read-only).

## Affected predicates

All 47 (`actual_card` 29, `micro_rules` 13, `replay_rng` 5). Every
STATIC_FAIL is carried by exactly one bind; all other binds (engine commit
db134b97, provider identity, protocol 2.0.0, seeds, fixture bytes,
rules-seed binding, tape schema) match unchanged.

## Old → new source identity

- `engine-bridge/.../XmageFullGameSession.java`:
  `ce3127d8...` (R23 bytes = `origin/main` `54fb222e`, verified before
  replacement) → `3086009d...` (this workstream).

## Behavior-vs-bytes determination (diff `54fb222e` → HEAD, session file)

Purely additive, +46 lines: one import, one nullable field, one delegating
overload (existing constructor body untouched, delegates with null so the
restoration hook is skipped), one guarded hook call, two inert read
accessors (`restorationGame`, `restorationSeats`). Null-path behavior
identical by construction: every pre-existing caller uses the original
signature and observes byte-identical control flow. No seed, RNG, replay,
hidden-information, decision-projection, or card-implementation path
touched. New behavior is reachable only through the new overload, exercised
solely by the new restoration tests.

## Retention condition and rerun evidence (on new bytes)

- Full engine-bridge suite 178/178 green on final bytes (163 pre-existing,
  incl. all session consumers, plus 15 restoration tests).
- Predicate checker `bin/check_predicates.py`: 47/47 STATIC_PASS.
- Negative controls preserved (bridge fail-closed suites green).

## Manifest update

`RETENTION_PREDICATES.json`: 47 binds updated to the new SHA above (old
values verified before replacement; no other bind touched).
`RETENTION_PREDICATE_RESULTS.json` regenerated by the authorized checker;
static 47/47 required.

## Disposition

No fixture/status change: `N_SCOPED_DISPOSITION_R21.json`,
`WORKLOAD_DERIVATION.json`, and the predicate set are untouched. WS2 adds a
construction capability only; no mapping classification awarded.

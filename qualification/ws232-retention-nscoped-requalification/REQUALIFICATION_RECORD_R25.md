# REQUALIFICATION RECORD — WS232 predicates, R25 re-baseline (2026-09-22)

Authorized by: Phase C authority grant (executor workstream:
native-procedure + decision-script executor), branch
`cpl/full107-procedure-executor-20260922` (sole writer; all other
worktrees/branches read-only).

## Affected predicates

13 (`micro_rules` set). Every STATIC_FAIL is carried by exactly one bind;
all other binds (engine commit db134b97, provider identity, protocol 2.0.0,
seeds, fixture bytes, rules-seed binding, tape schema, session-file R24
baseline) match unchanged.

## Old → new source identity

- `engine-bridge/.../XmageFullGamePlayer.java`:
  `b1cfca3c...` (R23 bytes = `origin/main` `57caa2dc`, verified before
  replacement) → `243ba8f6...` (this workstream).

## Behavior-vs-bytes determination (diff `57caa2dc` → HEAD, player file)

Surgical +15 lines in `priority()` consumption only: a selected
`SpellAbility` is now routed to the engine cast path
(`cast(spell, game, false, approvingObject)`, in-thread, with engine-owned
timing/costs/payment/targets/resolution and bookmark rollback on failure)
instead of falling into `activateAbility()`, which never casts spells onto
the stack. Offering is untouched (engine-enumerated `getPlayable` options);
every non-spell selection follows the byte-identical path. No seed, RNG,
replay, hidden-information, mulligan, or card-implementation path touched.

## Retention condition and rerun evidence (on new bytes)

- Full engine-bridge suite 180/180 green on final bytes (178 pre-existing
  incl. all priority consumers — generic submission, mulligan drive,
  projection negatives — plus 2 TAX execution tests).
- Predicate checker `bin/check_predicates.py`: 47/47 STATIC_PASS.
- First native-procedure executions green on final bytes (TAX-2 2P, TAX-4
  4P: construction MATCH + scripted cast with engine-owned {4} tax payment
  + terminal postconditions).

## Manifest update

`RETENTION_PREDICATES.json`: 13 binds updated to the new SHA above (old
values verified before replacement; no other bind touched).
`RETENTION_PREDICATE_RESULTS.json` regenerated by the authorized checker;
static 47/47 required. WS17 hash manifests regenerated for the touched
retention files.

## Disposition

No fixture/status change: `N_SCOPED_DISPOSITION_R21.json`,
`WORKLOAD_DERIVATION.json`, and the predicate set are untouched. Mapping
promotion of executed fixtures is a separate adjudicated step (not this
record).

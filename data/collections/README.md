# Collection snapshots

`current_deck_allocations.json` is a minimal simultaneous-allocation snapshot for the two current
100-card decks. It contains exactly 200 required copies and is used to test cross-deck quantity
validation. It is not the user's full physical inventory and does not replace the canonical inventory
workbook.

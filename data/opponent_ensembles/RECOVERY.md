# Opponent Ensembles Recovery

1. Restore the repository or Git bundle for package version 1.7.0.
2. Verify the Git commit and run the test suite.
3. Inspect `data/opponent_ensembles/ENSEMBLE_INDEX.json`.
4. Validate each `*-ensemble-v*.json` through `validate_ensemble`.
5. Treat all generated matchup scores as structural estimates.
6. Never merge assumed cards into observed cards or overwrite official profiles automatically.

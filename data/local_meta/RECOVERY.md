# Local Meta Recovery

Raw games are append-only under `data/local_meta/games/`. Corrections require a new game ID and `correction_of`. Backups are written per ingested hash. Exported scenarios are reproducible JSON.

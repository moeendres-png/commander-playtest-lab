# Java matrix

Only OpenJDK 21.0.10 is installed in the sandbox. JDK 8, 11 and 17 could not be installed because package/network access is unavailable. No XMage or Forge source tree exists, so build/runtime compatibility tests are `not_run`.

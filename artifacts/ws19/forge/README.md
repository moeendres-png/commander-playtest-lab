# WS-19 Forge post-WS17R restart

This directory contains candidate-specific source locks and controlled-salvage provenance only. Canonical runtime evidence is generated from the post-WS17R baseline and is never copied from the pre-WS17R branch.

Forge remains GPL-side and outside the proprietary Python package. The proprietary side is transport/schema validation only; the Forge-side provider is a separate JVM process.

Pre-WS17R evidence status: `PROVISIONAL_PRE_WS17R_NOT_ADMISSIBLE`.

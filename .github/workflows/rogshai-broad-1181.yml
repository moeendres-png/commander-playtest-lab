name: RogShai Broad 1.18.1 Full Rerun Superseded

on:
  push:
    branches:
      - runs/rogshai-broad-1181-20260813

permissions:
  contents: read

concurrency:
  group: rogshai-broad-1181-20260813
  cancel-in-progress: true

jobs:
  superseded:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Record supersession
        run: |
          echo 'Full 1.18.1 re-simulation superseded by run 31690917544.'
          echo 'Reason: current frontier identity matched the immutable 1.18.0 structural control; three current 1.18.1 sampled simulations matched raw paired observations, paired metrics, and exact seed-set hashes; current cohort re-diagnosis passed with no UNKNOWN informativeness states.'
          echo 'No canonical deck mutation was performed.'

{"evidence_class":"MODELED","note":"Topology proof rides on the AF01 handshake implementation (same successor) plus deployment-specific negatives. One plan per declared topology; evidence never transfers across topologies.","plans":{"containerized_service":{"extra_evidence":["image provenance verification (mismatch refuses start)","no secret baking (scan)","DISPLAY removed (Forge) / headless set (XMage)"],"tests":["T1 handshake inside container image","T2 per-cardinality lifecycle in container","T3 kill -9 child -> parent typed failure, no PASS, clean relaunch","T4 stale image (wrong SHA) refuses start (fail closed)","T5 replay fresh-process in container"]},"local_child_process":{"tests":["T1 AF01 handshake on child stdio transport","T2 per-cardinality full lifecycle (create/start/decide/submit/result/close)","T3 child crash (SIGKILL mid-decision) -> typed provider failure, no fabricated outcome, clean relaunch","T4 timeout -> terminal DECISION_TIMEOUT/STALE_REVISION, game cannot continue","T5 protocol desync (version mismatch / malformed line) -> typed error before engine contact","T6 orphan cleanup (shutdown verbs; no residue; port/state clean)","T7 fresh-process replay per cardinality"]},"localhost_service":{"extra_evidence":["loopback-only binding proof (no remote listen)","port-reuse refusal"],"tests":["same as child + T8 service restart mid-game -> typed failure + clean recovery + stale rejection"]},"packaged_sidecar":{"extra_evidence":["packaged-version -> engine-identity mapping record","update/downgrade binding (stale sidecar fails closed)"],"tests":["same as container"]}},"schema_version":"ws230-topology-test-plan/1.0.0"}

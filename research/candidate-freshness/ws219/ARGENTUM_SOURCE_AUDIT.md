# ARGENTUM Source Audit — WS219 (exact pin 3f46367d)

Source lock: `wingedsheep/argentum-engine` commit `3f46367d87c88bcf156a843a9e69fd29e1693872`, tree `2adf51caa8f9c6a9908ea961fa988ebb5eb1959f`, read-only root `/home/moeen/code/ws219-ref-argentum-3f46367d`. Verified via `git rev-parse HEAD` + `HEAD^{tree}`. No writes made to candidate. License: MIT (`LICENSE`, sha256 `95b0f7d5…`). All verdicts CODE_DERIVED unless probe says DIRECTLY_VERIFIED.

## 1. Architecture

- Immutable pure core: `rules-engine/.../state/GameState.kt:359-381` (`val rng/nextEntityId/nextRoutingId`, `data class GameState`, `withEntity()=copy(…)`). Doc `docs/architecture-principles.md:319-398` (`(GameState,GameAction)->ExecutionResult(state,events,error,pendingDecision)`).
- Entry: `core/ActionProcessor.kt:42,74` (`process(state,action):ProcessedAction`, atomic-on-error `L119-135`). Legal surface: `legalactions/LegalActionEnumerator.kt:64-88` (`enumerate(state,playerId,mode)` + combat-exclusive branch). Init: `core/GameInitializer.kt:154,161-162` (`initializeGame(GameConfig)->InitializationResult`).
- Gym API (not `observe()/step()` on engine directly): `gym/.../GameEnvironment.kt:165,193,253,285,306` (`reset/step/stepExactlyOne/legalActions/fork/restore`); `gym/.../GameGymEnv.kt:36-65` (`observe(revealAll?)/step/submitDecision/fork/snapshot/restore`); `gym/.../service/MultiEnvService.kt:56-120`.
- Layout: `mtg-sdk` (pure data) / `mtg-sets/<era>` (10 era modules) / `rules-engine` (zero server deps) / `ai` / `gym+gym-server+gym-trainer` / `game-server` / `web-client` (dumb terminal). ECS+projection (`architecture-principles.md:425-665`): components, `ObjectRef(entity,generation)`, LKI `EntitySnapshot`, Rule 613 base vs `projectedState` 7 layers + 613.8 trial-sort, serializable `Suspension(question,answer)`.

## 2. Rules completeness (broad, Commander+multiplayer present, gaps declared)

- Costs/mana: implemented (`RULES.md:290-318`; `mechanics/mana/{CostCalculator.kt:153,174-197,ManaSolver.kt,ManaPaymentWindow.kt}`; suspension + mana-ability reopen).
- Priority/APNAP/turn: implemented (`RULES.md:343`; `GameInitializer.kt:253-290` team-aware shuffle; `CommanderZoneChoiceCheck.kt:30` APNAP-by-turn-order).
- Stack: implemented (`mechanics/stack/StackResolver.kt:395-403` tax on cast-commit, `:3741-3743` command-zone move; `FinishResolvingSpellContinuation`).
- Combat: implemented (`mechanics/combat/CombatDamageManager.kt:1467,1781-1785` reflection counts, commander-damage accumulation, token copies excluded per 903.10a; `GameState.kt:333-339`, `GameConfig.kt:70` attack-mode).
- Targeting: implemented (`mechanics/targeting/{TargetValidator.kt,HexproofSuppression.kt,PlayerProtectionRules.kt}`; late-binding `ContextTarget/BoundVariable`, 608.2b recheck).
- Triggers: broad list (`RULES.md:245-289`); `TriggerDetector.detectTriggers`; look-back-in-time batching; LKI single `EntitySnapshot`.
- Replacements: PARTIAL. Model `ReplacementEffect.appliesTo:EventPattern` + `activeReplacementChain` 614.5; only draw domain migrated to central `ReplacementEffectProcessor` — "others still run through their own dispatchers" (`arch:873-878`). Dredge intrinsic, `Duration.NextUse` Words shields.
- Layers: implemented (`StateProjector` 613 layers + 613.8 dependency trial; must pass `projectedState` to `predicateEvaluator.matches`).
- SBAs: implemented standard + commander + team (`mechanics/sba/{SbaOrder.kt,StateBasedActionCheck.kt,player/{PlayerLifeLossCheck,PoisonLossCheck,CommanderDamageLossCheck.kt:27,PlayerLeavesGameCheck/Processor},permanent/{LethalDamageCheck,ZeroToughnessCheck,CommanderZoneChoiceCheck.kt:17-42}}`).
- Zones/Copy/Control: implemented (zones incl. `COMMAND/STACK/SIDEBOARD`; copy Layer-1; control via `ControllerComponent`/Layer-2 + `GainControl`; linked exile, face-down `TurnFaceUp`, transform, `ZoneTransitionOutcome PRIMARY/REPLACEMENT_ADDITIONAL/DURATION_RETURN`).
- Extra turns/skips: implemented (`RULES.md:230-231` TakeExtraTurn/SkipNextTurn/SkipUntap/SkipCombat/AddCombat/HijackNextTurn; `GameState.kt:217-218`).
- Elimination: implemented (`PlayerLeavesGameProcessor/Check`, `TeamLossPropagationCheck.kt:17-18`, FFA continue-on-concede + standings `data-contracts.md:508-521` CR800.4a/104.2a; 2HG `playersWinLoseAsTeam` vs TvT individual `Format.kt:50-58`).
- Mulligan/start: London + CR800.6 free mulligan `>2P` (`GameInitializer.kt:222` `freeMulligan=config.players.size>2`; `PlayerComponents.kt:337-352` `cardsToBottom=max(0,taken-1)`; `MulliganHandler.kt:18-24,124-125` shuffle+redraw via `nextRandom`).
- Commander tax/zone/damage: implemented 1v1→pod (`Format.kt:81-88` Commander threshold=21/deckSize=100/life=40; `identity/{CommanderComponent.kt:8-27,CommanderRegistryComponent.kt:8-15}`; `CostCalculator.kt:153,181-197` tax; `CastFromZoneEnumerator.kt:99-114` + `CastZoneResolver.kt:122-133` owner-only 903.8; `CommanderZoneChoiceCheck.kt:17-42` 903.9a; `CommanderDamageLossCheck.kt:27-36` + `CombatDamageManager:1781` 903.10a non-token; `ClientDTO.kt:838-905` tallies).
- Partner/Background: MISSING (declared, DIRECTLY_VERIFIED). `GameInitializer.kt:38-44` "Phase 1 supports a single commander; partner/Background pairings are Phase 4"; `PlayerConfig.commanderCardName:String?` singular; `DeckValidator.kt:323` Partner unsupported; `CommanderRegistryComponent.commanderIds:List` reserved but init stamps one; `backlog/commander-format.md` Phase-4.
- 2-6P FFA (+teams): implemented (`GameInitializer.kt:155` `>=2`, no hard cap; `README.md:131-140` 2-6 FFA CR806, elimination rail, rematch; `data-contracts.md:434-492` TOURNAMENT/FREE_FOR_ALL, `attackMode MULTIPLE/LEFT/RIGHT`, TwoHeadedGiant 4p shared 30 life, TeamVsTeam 4/6/8 individual 20 life; `Format.kt:63-67` Commander table-size neutral).

## 3. Legal-action surface + AI/GUI seams

- `GameAction` families (`core/GameAction.kt:19,33-649`): `CastSpell(targets/modeIndex/xValue/chosenCosts/payment/alternativeCost)` (one alt-cost CR118.9a), `ActivateAbility`, `PlayLand(asBackFace 712.12)`, `DeclareAttackers/DeclareBlockers/OrderBlockers`, `ChooseManaColor`, `SubmitDecision(decisionId,choiceIndex/selectedIds)`, `TakeMulligan/KeepHand`, `Concede`, `PassPriority`. `LegalAction` (`LegalAction.kt:14-224`) carries `validTargets/targetRequirements/xConstraints*`, combat (`validAttackers/mandatoryAttackers/validAttackTargets/validBlockers/blockerMaxBlockCounts/mandatoryBlockerAssignments`), costs (`manaCostString/hasXCost/maxAffordableX/additionalCostInfo`, convoke/delve/improvise/waterbend/harmonize/craft/tapForPower/modal).
- Enumerators (22): `LegalActionEnumerator.kt:31-53` — PassPriority, PlayLand, Morph, CastSpell (+conspire/casualty/splice/kicker/cleave/modal/secondary-face), Sneak/Emerge/WebSlinging, Cycling, Plot, Foretell, Suspend, CastFromZone (command/top-of-library/exile-linked/graveyard/flashback/mayhem/disturb/harmonize/warp/dash/forage), ManaAbility (CR605.3a), TurnFaceUp, UnlockRoom, ActivatedAbility, Crew, Saddle, ZoneActivated, CommandZoneAbility; combat-declaration exclusivity; `EnumerationMode.ACTIONS_ONLY` skips auto-tap for sim.
- Decisions: 17 `PendingDecision` (ChooseTargets/SelectCards/YesNo/BatchYesNo/ChooseMode/ChooseColor/ChooseNumber/Distribute/OrderObjects/SplitPiles/ChooseOption/ChooseReplacement/AssignDamage/SearchLibrary/ReorderLibrary/SelectManaSources/BudgetModal/CombatResolution) + 17 `DecisionResponse`; gym folds simple to action-IDs, complex → `requiresStructuredResponse` + `submitDecision`.
- AI-only seams (in `:ai`, not engine): `ai/.../GameSimulator.kt:20-110` (simulate/simulateDecision/getLegalActions, auto-pass+trivial-resolve, `decisionResolver`+`FastDecisionResponder`), `DecisionResponder.kt:36-574` (heuristic targets/distribute/order/mana), `AIPlayer.kt:64-160` (illegal→`PassPriority`/mandatory-combat fallback), `MeaningfulActionFilter`, `Strategist`, `BoardEvaluator`, `Determinizer/OpponentModel`, draft/deckbuild advisors. `GameEnvironment.playGame` defaults missing selector to `actions.first{affordable}` — harness convenience, not engine rule.
- Excludability: engine+enumerator clean (`rules-engine/build.gradle` deps = `:mtg-sdk` only; `stepExactlyOne` bypasses `GameSimulator` and hits `ActionProcessor` directly `:222-239`). Gym NOT clean: `gym/build.gradle: implementation(project(":ai"))` and `GameEnvironment.kt:3-14` imports `ai.engine.*` — excluding built-in AI from gym path requires refactor. No first-option/random-option/default-yes-no inside engine; server `GameStallGuard` draw backstop is liveness, not legality.
- GUI-only: none in rules path. `web-client` dumb terminal; server authoritative; `game-server/build.gradle` pulls `:rules-engine+:mtg-sdk+:mtg-sets+:ai+:oracle-assay`; topology `game-server:8080` (WebSocket+masking) vs `gym-server:8081` (thin HTTP `MultiEnvService`), no Redis/auth/TTL/byte-snapshots/metrics in gym-server by design.

## 4. Hidden information (implemented w/ documented scope limits)

- Owner: engine `view/Visibility.kt:30-42` single source of truth shared by client state, decision masking, AI determinization, Gym observations.
- Mandatory principal: `Visibility.kt:46-68,79-93,154-199` `isZoneVisibleTo(state,zoneKey,viewingPlayerId,isSpectator)` (`LIBRARY:false`; `HAND/SIDEBOARD: owner|actorFor|teammate|reveal-effect`); `isCardIdentityVisibleTo` (+face-down CR708.5, `RevealedToComponent`, top-of-library grants, foretell/may-play/may-look exile grants `222-231`).
- Projection: `ClientStateTransformer.kt:102-135,153-213` `transform(state,…,viewingPlayerId,isSpectator)` masks via `Visibility`; own-deck-only `deck` + `remaining`=unseen-count anti-leak; spectator gets public reveals only.
- Gym: `contract/ObservationBuilder.kt:62-82,190-229,332-383` `build(state,perspectivePlayerId,legalActions,revealAll=false)`; `revealAll` "debug scripts only; never for real self-play"; hidden zones `hidden=true,size=true-count,cards=known-subset`; stack face-down masked CR708.4/5; emblems known gap (`TrainingObservation.kt:19-21` zone-less, no view).
- Reveal/look/search: `RevealedToComponent`, `RevealedInHandTracker`, `LibraryRevealUtils.placementAudience`, `MayLookAtInExileComponent`, `hasMayPlayFor` linkage.
- AI determinization shares authority: `engine/hidden/{HiddenSlotRewrite.kt,HiddenWorldMaterializer.kt:33-52}` + `ai/.../hidden/Determinizer.kt`; material gap `arch:496-509` — in-flight pin conservative-superset, `grantedKeywordAbilities/mayPlayPermissions/lastCardDrawnThisTurnByPlayer` out of scope (dangling ref).
- Leakage tests: `engine/view/*VisibilityTest` (19 files), `FaceDownGameLogMaskingTest`, `CombatDamageFaceDownNameLeakTest`, `MidGameEliminationVisibilityTest`, `gym/.../ObservationVisibilityTest`, `ObservationProjectedStateTest`, `game-server/.../GameMaskingTest`.

## 5. Determinism/RNG/replay

- RNG authority: `mtg-sdk/.../model/GameRng.kt:1-21` SplitMix64 pure immutable (`nextLong/nextInt unbiased/nextBoolean/nextDouble/shuffle/pick/split`); `GameState.kt:358-366,1205-1210` `val rng:GameRng`, `nextRandom(draw)` threads back; `GameInitializer.kt:85-91,161-162` `GameConfig.seed:Long?`, null→`System.nanoTime()` ("one sanctioned non-determinism boundary", seed recorded on result).
- Identity determinism: `GameState.kt:368-381` `nextEntityId` (`e0,e1…`, not UUID) + `nextRoutingId`; `arch:400-424` replay reproduces routing tokens; `NoWallClockInGameLogicTest.kt:1-40` bans wall-clock/UUID in engine (timestamps via `state.tick()`); `ReproducibilityTest.kt:13-60` asserts same-seed byte-identical `GameState`.
- Snapshot/fork/digest: `GameEnvironment.kt:285-313` + `SnapshotCodec.kt:17-50` O(1) reference swap, in-process `Slot` only ("sealed design leaves room for byte-blob… once we need"); `MultiEnvService.snapshot/restore` same; `contract/StateDigest.kt:1-50` SHA-256 over stable fields (excl. legalActions) for MCTS transposition; `schemaHash` fail-fast.
- Semantic replay (not frame archive): `data-contracts.md:635-789` `CompactReplay{setup(seed,decks,seats,format/teams/attackMode)+actions}` + `ReplayReconstructor` folds via `ActionProcessor`; `DecisionResponse.withDecisionId` rebinding; pins JSON per deck; checkpoints every 20 (`ReplayFingerprint`); archived `presentation` fallback with `ReplayFidelity EXACT/UNVERIFIED/DIVERGED`; v2 tolerant, `MAX_RECORDED_ACTIONS=25k`, cap below `GameStallGuard 50k`; ECL 40-card ~107 KB total. This is in-process snapshot+action-fold replay, NOT clean-process semantic replay under Commander-Lab definition (no cross-process byte-form proof; `SnapshotCodec` Slot-only).
- Event/decision tapes: typed `GameEvent`s + `TriggerDetector`; `DecisionRequestedEvent` on `suspendForDecision`; `ContinuationHandler.resume` ID-checked; live epoch anti-stale; legacy suspension reader + `suspension-traces/` regression.

## 6. Card implementation coverage (large partial, tooling-gated, fail-closed)

- Model: pure-data `cardDef{}` DSL, no card logic in engine (`AGENTS.md:127-138`); facades `Effects.*` + `Patterns.*` enforced by `FacadeBoundaryTest`.
- Count (DIRECTLY_VERIFIED via probe `probes/ws219_argentum_card_check.py`): `distinct_card_names=13915` `card("…")` names; ~12,967 files w/ `val=card(`, ~13,503 `card(` defs across 10 era modules. Per-set baked denominator `game-server/.../resources/coverage/{set-totals.json:6.2MB,set-products.json,assay-verdicts.json:3.9MB,implementation-history.json}`; live tracker `card-implementation-progress.html`.
- Tooling: `just coverage…` (predictive non-authoritative), `just assay…` (bidirectional grammar; touchstone + differential gates); Assay explicitly not a loader except dev-gated Scenario Builder sandbox.
- Missing-card model: FAIL-CLOSED. Unimplemented = absent from `CardDiscovery` scan; `GameInitializer:175-180` `cardRegistry.requireCard(commander)` fail-fast; `ScenarioRequest` rejects unknown names 400. LLM-AI unparseable → heuristic play is pilot fallback, not rules fallback.
- Declared exclusions: `coverage/card-exclusions.json:31-44` 12 cards (9 ante, `Shahrazad` subgame, `Chaos Orb/Falling Star` dexterity) — none of frozen 29 excluded.
- Frozen-29 probe result (DIRECTLY_VERIFIED): 11 `card()` defs found (`Wash Away`, `Dig Through Time`, `Finale of Revelation`, `Shriekmaw`, `Syphon Mind`, `Gratuitous Violence`, `Bolt Bend`, `Makeshift Mannequin`, `Warstorm Surge`, `Basilisk Collar`, `Path of Ancestry`); 18 MISSING (all 4 Partner commanders, Veyran, Harmonic, Narset-Parter, Jeska-Thrice, Magma Opus, Wear//Tear, Flare, Vandalblast, Psychosis, Kaervek-Merciless, Butcher, Burn, Find//Finality, Boseiju-Reaches). Of the 11, 5 have dedicated behavior tests (WashAwayScenarioTest, DigThroughTimeTest, FinaleOfRevelationScenarioTest, SyphonMindTest, MakeshiftMannequinScenarioTest) → SUPPORTED; 6 have cardDef+snapshot but zero dedicated tests (Shriekmaw, Gratuitous, BoltBend, Warstorm, Collar, Path) → PARTIAL. Import/readback gets zero behavior credit. Strongest blocker: all four Partner commanders have no `card()`/snapshot AND engine hard-blocks Partner anyway, so no Partner-commander game can initialize.

## 7. Tests (inventory, NOT RUN as suite)

- `rules-engine`: 587 files (Core 11 incl. `ReproducibilityTest,RoutingIdentityTest,SuspensionOwnershipTest,CommanderSetupTest`; handlers 44; mechanics 16; multiplayer 19 incl. `CommanderPodTest,AttackModeTest,LeaveTheGameTest,MultiDefenderCombatTest,MultiplayerMulliganTest,MultiplayerSmokeTest,TeamVsTeamTest,2HG×6`; legalactions 23; view 19; hidden 2; hygiene 9; mana 4; triggers 7; event 8; scenarios 398).
- `game-server`: 85 files (`GameMaskingTest,GameMulliganTest,GameFlowTest,MultiplayerSessionTest,TwoHeadedGiantSessionTest`, replay `{CompactReplayReconstructionTest,ReplayDurabilityTest,ReplayTruncationTest,ReplayStorageTest,ReplayDivergenceReproTest}`, persistence, scenarios).
- `gym`: `GameEnvironmentTest,GameGymEnvCombatTest,GameEnvironmentExactlyOneSubmissionTest,TrainingObservationTest,ObservationVisibilityTest,ObservationProjectedStateTest,MultiEnvServiceTest,EnvWorkerPoolTest,DeckbuildEnvironmentTest`; `ai`: `DeterminizerInvariantsTest,XCostSelectionTest,PlayoutEngineTest,AutoPassParityTest,CommanderDeckGeneratorTest,arena/*,training/*`.
- `mtg-sets` scenario corpus: 3,528 files; `e2e-scenarios/tests/{general,portal,khans,rav,lorwyn,…,duskmourn}` Playwright + `manual-scenarios/*.json`.
- Frozen-card one-to-one: no `AFxx`/frozen/29-card list in repo; closest `SetCoverageStandardTest`, `ai/.../FrozenBaselineTest` (AI baseline, not card freeze).

## Bottom line

Immutable, pure-state, fail-closed broad engine with credible Commander/multiplayer/FFA implementation, 22-family legal-action enumerator, principal-mandatory visibility, seeded SplitMix64 RNG, and ~13.9k card names defined — all CODE_DERIVED except cardDef census + Partner gate + OVERLOAD absence which are DIRECTLY_VERIFIED by probe/grep. Material gaps to Full-Rules: 18/29 frozen cards MISSING (incl. all Partner commanders, blocked at init); replacement non-draw domains not on central processor; hidden-slot side-lists + emblems incomplete; gym→ai compile coupling; snapshot/Slot-only (no clean-process semantic replay byte form); zero runtime execution in this workstream (CODE_DERIVED≠RUNTIME_VERIFIED); OVERLOAD mechanic absent (`Keyword.kt` has no OVERLOAD; Vandalblast missing).
